name: Deploy Local Docker

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code
        run: git pull origin main

      - name: Rebuild backend and frontend
        run: docker compose up -d --build backend frontend