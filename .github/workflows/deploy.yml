name: Deploy Fullstack Local

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Go to project folder
        run: |
          cd D:\VDT_1\mini_project\documentmanagement
          git pull origin main
          docker compose up -d --build backend frontend