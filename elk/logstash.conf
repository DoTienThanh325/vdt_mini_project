input {
  beats {
    port => 5044
  }
}

filter {
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "documenttransfer-logs-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}