filebeat.inputs:
  - type: filestream
    id: documenttransfer-logs
    enabled: true
    paths:
      - /app/logs/*.log
    parsers:
      - ndjson:
          target: ""
          add_error_key: true

output.logstash:
  hosts: ["logstash:5044"]